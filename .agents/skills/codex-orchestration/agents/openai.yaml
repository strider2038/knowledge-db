interface:
  display_name: "Codex orchestration"
  short_description: "Orchestrate within the session model and reasoning ceiling"
  default_prompt: "Use $codex-orchestration to plan, implement, verify, review, and close out this project change."

policy:
  allow_implicit_invocation: false
