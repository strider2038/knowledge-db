## 1. Зависимости и конфигурация Vite

- [x] 1.1 Добавить `vite-plugin-pwa` (и при необходимости типы) в `web/package.json`, установить зависимости
- [x] 1.2 Подключить плагин в `web/vite.config.ts`: manifest (name, short_name, theme/background colors, display, start_url), `registerType`, исключение или `NetworkOnly` для префикса `/api`, отключение/ограничение SW в dev (`devOptions`)
- [x] 1.3 Убедиться, что `build.rollupOptions` (в т.ч. `inlineDynamicImports`) совместимы с precache; при конфликте задокументировать или скорректировать конфиг

## 2. Ассеты и HTML

- [x] 2.1 Добавить иконки PWA в `web/public/` (минимум 192×192 и 512×512, при необходимости maskable) и связать их в manifest плагина
- [x] 2.2 Обновить `web/index.html`: при необходимости `theme-color`, ссылки на apple-touch-icon / meta для установки на iOS (если принято в дизайне)

## 3. Проверка сборки и встраивания

- [x] 3.1 Выполнить `npm run build` в `web/`, проверить наличие манифеста и регистрации SW в `dist/`
- [x] 3.2 Собрать kb-server и убедиться, что статика с PWA-файлами отдаётся без 404 (тот же механизм embed, что для текущего UI)
- [x] 3.3 Прогнать `npm run lint` и `npm run test` в `web/` после изменений

## 4. Документация change

- [x] 4.1 Кратко зафиксировать в комментарии к PR или в описании задачи: ограничение «данные только при работающем API», поведение обновления SW
