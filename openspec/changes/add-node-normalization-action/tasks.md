## 1. API и оркестрация нормализации

- [ ] 1.1 Добавить API endpoint запуска нормализации узла через Cursor Agent и контракт статуса операции.
- [ ] 1.2 Реализовать серверное формирование промта по текущему узлу и запуск Cursor Agent с проверкой `CURSOR_API_KEY`.
- [ ] 1.3 Реализовать post-step `sync` после успешной нормализации и возврат итогового статуса (success / sync error).
- [ ] 1.4 Добавить API-тесты для успешного сценария, ошибок конфигурации и ошибки шага sync.

## 2. UI действия «Нормализация» на странице узла

- [ ] 2.1 Добавить кнопку «Нормализация» в Action Bar страницы узла в соответствии с правилами видимости.
- [ ] 2.2 Подключить вызов API нормализации, состояния loading/disabled и обработку статусов/ошибок.
- [ ] 2.3 Добавить отображение результата операции и обновление данных узла после успешного завершения.
- [ ] 2.4 Добавить/обновить frontend тесты для действия «Нормализация».

## 3. Runtime и документация

- [ ] 3.1 Обновить Dockerfile: установка Cursor Agent через `curl https://cursor.com/install -fsS | bash`.
- [ ] 3.2 Добавить поддержку `CURSOR_API_KEY` в конфигурации приложения и валидацию наличия для endpoint нормализации.
- [ ] 3.3 Обновить `README.md` и `.env.example` с описанием `CURSOR_API_KEY` и требований к контейнерному запуску.
- [ ] 3.4 Выполнить проверку change (`openspec validate`, `openspec status`) и зафиксировать готовность к apply.
