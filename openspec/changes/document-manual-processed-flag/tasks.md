## 1. Модель и хранение (internal/kb)

- [ ] 1.1 Добавить поле `manual_processed` в структуру метаданных узла (чтение/запись frontmatter), трактовать отсутствие как `false`.
- [ ] 1.2 Расширить валидацию: опциональный boolean; при неверном типе — ошибка валидации.
- [ ] 1.3 Убедиться, что `kb-cli validate` принимает узлы без поля и с `manual_processed: true/false`.

## 2. REST API

- [ ] 2.1 Включить `manual_processed` в JSON узла (GET по path) и в элементы списка `GET /api/nodes`.
- [ ] 2.2 Реализовать query-параметр `manual_processed` для списка (`true` / `false`; без параметра — все).
- [ ] 2.3 Поддержать обновление `manual_processed` в существующем эндпоинте сохранения метаданных узла (или добавить минимальный эндпоинт, согласованный с текущим API).
- [ ] 2.4 API-тесты: список с фильтрами, чтение узла, обновление флага, 400 при неверном типе.

## 3. Веб: обзор (overview-browse)

- [ ] 3.1 Добавить UI-фильтр «Все / Проверено вручную / Не проверено» и прокинуть `manual_processed` в запрос списка.
- [ ] 3.2 Сохранять и восстанавливать `manual_processed` в URL query; включать в `returnTo` при переходе на узел из таблицы.

## 4. Веб: страница узла

- [ ] 4.1 Отобразить чекбокс «Проверено вручную», синхронизированный с `manual_processed` из API.
- [ ] 4.2 Сохранять изменение флага через API метаданных (с обработкой ошибок и откатом UI при сбое).

## 5. Финализация

- [ ] 5.1 Обновить типы/клиент API во `web/` при необходимости.
- [ ] 5.2 `go build ./... && go test ./... -race`; `cd web && npm run build` (и lint при наличии).
