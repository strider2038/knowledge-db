## 1. Логика раскрытия и хранилище

- [x] 1.1 Вынести чистые функции: глубина пути, префиксы-предки для `path`, вычисление «раскрыт ли узел» по правилам (must / default depth / user).
- [x] 1.2 Реализовать загрузку и сохранение в `localStorage` (версионированный ключ): `defaultExpandDepth`, `userExpanded`, `userCollapsed`.

## 2. UI обзора

- [x] 1.3 Добавить хук `useTopicNavExpansion` и встроить в `OverviewPage`: chevron для веток с детьми, клик по названию — только выбор `path`.
- [x] 1.4 Добавить компактный контроль «уровней по умолчанию» в блоке «Темы» (десктоп и sheet на мобильных).
- [x] 1.5 Обновить/дополнить тесты `OverviewPage` при необходимости; прогнать `npm run build` и тесты web.
