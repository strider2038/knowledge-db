// Package googleoauth implements Google OAuth2 (authorization code) helpers: signed state,
// token exchange, userinfo, email allowlist, and safe redirect path building.
// It is transport-agnostic except for optional HTTP redirect helpers.
package googleoauth
